int a,b,c;
int main()
{
	int idx,qty;
	double average;
	int nums,sum;
	sum=0;
	for(idx=0;idx<nums;idx=idx+1)
		{sum = sum +qty;}
	average = sum / nums;
	if(average < 10.0)
		{a=10;}
	else
		{b=20;}
	c=30;

}
