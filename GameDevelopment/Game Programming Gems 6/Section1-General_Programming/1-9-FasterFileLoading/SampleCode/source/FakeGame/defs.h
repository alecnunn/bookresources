#ifndef _DEFS_H_
#define _DEFS_H_

#include <core_defs.h>

#include <list>
#include <string>
#include <map>
typedef std::list<std::string> FileList;
typedef std::map<std::string, uint8*> DataMap;


#endif
